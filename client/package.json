{
  "name": "last-shift-mcp-client",
  "version": "0.1.0",
  "private": true,
  "description": "MCP client + MCP Apps host for Ostatnia Szychta (The Last Shift) - the thin demo shell standing in for Alexa+'s conversational surface.",
  "type": "module",
  "scripts": {
    "verify": "tsx src/verifyConnection.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "7.0.2"
  }
}
